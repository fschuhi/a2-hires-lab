# a2-hires-lab — Windows-only requirements
#
# Pulls in the shared base, then adds packages that only exist / are only needed
# on Windows. Selected automatically by the Makefile when OS=Windows_NT.

-r requirements.txt

# Nothing Windows-only is needed today. PyXLL goes here when the papple2
# bridge becomes real (see TODO.md).
